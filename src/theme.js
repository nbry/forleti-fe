import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#7be0ad",
    },
    secondary: {
      main: "#e2e2df",
    },
    danger: {
      main: "#ffadad",
    },
    warning: {
      main: "#fdffb6",
    },
    netural: {
      main: "#a0c4ff",
    },
  },
});

export default theme;
